pipeline {
  agent {
    kubernetes {
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'echo hello jenkins !!!!'
        sh 'echo 转发是否成功1'
        sh 'echo 转发是否成功2'
        sh 'echo 转发是否成功3'
        sh 'echo 转发是否成功4'
      }
    }
  }
}
